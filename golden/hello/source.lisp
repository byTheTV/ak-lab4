; Golden «hello» (минимальный): вывод «Hi» + перевод строки через порт DATA_OUT.
; Строковые литералы в codegen пока не поддержаны — явные ASCII-коды (pstr — отдельная фаза).

(progn
  (out 72)   ; H
  (out 105)  ; i
  (out 10))  ; \n
